# Dependabot:只开 GitHub Actions 生态的每周巡检。
# 本仓库为 ESP-IDF 固件工程，无 npm/Node 依赖，故不配 npm 生态。
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - '*'
